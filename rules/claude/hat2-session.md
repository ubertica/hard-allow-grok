# HAT2 session (light stamp — no nuclear catalog in Claude global rules)

HAT2 active when `HAT2_ACTIVE=1` / live HA token. Full law: `~/.hat2/SESSION-RULES.md`.
Planes: `~/.hat2/CANONICAL-PLANES.md`. Nuclear grants on disk for Grok HA / hat2 multi.
TUI: `hat2 --isolated` → `CLAUDE_CONFIG_DIR=~/.hat2/claude-profile`.

<!-- armed by ~/.hat2/arm.mjs (default: no global nuclear prose) -->
